--- a/litecross/Makefile
+++ b/litecross/Makefile
@@ -88,6 +88,11 @@ FULL_GCC_CONFIG = --enable-languages=c,c++ \
 FULL_MUSL_CONFIG = $(MUSL_CONFIG) \
 	--prefix= --host=$(TARGET)
 
+ifeq ($(STATIC_HOST_COMPILER),y)
+FULL_GCC_CONFIG += $(if $(HOST),--with-host-ldflags=-static)
+FULL_BINUTILS_CONFIG += $(if $(HOST),--with-host-ldflags=-static)
+endif
+
 ifneq ($(NATIVE),)
 HOST:=$(TARGET)
 endif
